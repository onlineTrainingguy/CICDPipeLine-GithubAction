name: Hello World Workflow
on:
    push:
        branches:
        - main
jobs:
    hello_world:
     runs-on: MyAgent
     steps:
        - name: Print Hello World
          run: echo "Hello, World!"
        - uses: actions/checkout@v4  